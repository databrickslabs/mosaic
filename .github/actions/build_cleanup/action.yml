name: cleanup build
description: cleanup build
runs:
  using: "composite"
  steps:
    - name: Remove tmp files
      shell: bash
      run : |
        # - remove local tmp files
        sudo rm -rf /tmp/mosaic_tmp *
        # - remove pseudo fuse files
        sudo rm -rf /dbfs/checkpoint/mosaic_tmp *
