name: build_scala
on:
  push:
    branches:
      - "scala/"
  pull_request:
    branches:
      - '**'
jobs:
  build:
    runs-on: ubuntu-20.04
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    strategy:
      matrix:
        python: [ 3.8.10 ]
        spark: [ 3.2.1 ]
        R: [ 4.1.2 ]
    steps:
      - uses: mosaic/build_ymls/scala_build@v1
      - uses: mosaic/build_ymls/upload_artifacts_build@v1
